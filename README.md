# Quizpoa 🚀

An AI-powered real-time quiz platform designed for educators and students. Generated by AI, managed by you.

## ✨ Features
- **AI Magic**: Generate 10-question quizzes instantly from any topic or text.
- **SaaS Aesthetic**: Clean, professional UI with Deep Slate and Electric Indigo branding.
- **Real-time Engine**: Synchronized game lobby, question timers, and live leadboards.
- **Accessibility First**: Geometric patterns on student answer cards for inclusive learning.

## 🛠️ Tech Stack
- **Frontend**: React (Vite), Tailwind CSS 4.0, Framer Motion, Zustand.
- **Backend**: Node.js, Express, Socket.io, OpenAI SDK.
- **State Management**: Zustand (Client), In-memory RoomManager (Server).

## 🚀 Quick Start

### 1. Clone & Install
```bash
git clone <your-repo-url>
cd quizpoa
cd client && npm install
cd ../server && npm install
```

### 2. Configuration
Create a `.env` file in `server/`:
```env
OPENAI_API_KEY=your_key_here
PORT=3001
```

### 3. Run Development
**Server:**
```bash
cd server && npm start
```
**Client:**
```bash
cd client && npm run dev
```

## 📦 Deployment
This project is configured for easy deployment on **Vercel**:
1. Connect this repository to Vercel.
2. Set the root directory to `client` (or use Vercel's monorepo settings).
3. Ensure the server is deployed to a Node.js compatible environment.

---
Built with ❤️ by **Eutycreatives**
